# Recipe: Create a New Agent

1. `mkdir -p agents/agent_myagent`
2. Create `manifest.json` with name, version, level, action, principles
3. Create action script (bash or python)
4. Register in `skills/registry.json`
5. Test: `bash agents/agent_myagent/script.sh "test"`
6. Document: create `README.md`
